# Place on data folder the XML data files from project gutenberg database.
# Run the following commands:

$ cd data
$ curl -O http://www.gutenberg.org/cache/epub/feeds/rdf-files.tar.bz2
$ tar -xvjf rdf-files.tar.bz2

